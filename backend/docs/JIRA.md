# Project Tickets

## [001] Initialize project structure for events platform
Set up the initial repository structure including Rust backend and Angular frontend. Add basic project scaffolding, dependencies, and .gitignore configuration to enable clean development workflow.

## [002] Implement basic Rust backend server with first endpoint
Create a minimal Rust HTTP server and expose the first endpoint for testing purposes. Ensure the server builds and runs locally with a simple response to validate setup.
